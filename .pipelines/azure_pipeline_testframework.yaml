# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
#

trigger: none
pr: none

# Define a reusable template for Testkube deployments
parameters:
  - name: environmentName
    type: string
  - name: resourceGroup
    type: string
  - name: clusterName
    type: string
  - name: clientId
    type: string

jobs:
- deployment: Testkube
  environment: container-insights
  timeoutInMinutes: 120
  displayName: "${{ parameters.environmentName }} tests"
  pool:
    name: Azure-Pipelines-CI-Test-EO
  variables:
    skipComponentGovernanceDetection: true
  strategy:
    runOnce:
      deploy:
        steps:
        - checkout: self
          persistCredentials: true

        - task: AzureCLI@1
          displayName: Get kubeconfig
          inputs:
            azureSubscription: 'ContainerInsights_Build_Subscription_CI'
            scriptLocation: 'inlineScript'
            inlineScript: 'az aks get-credentials -g ${{ parameters.resourceGroup }} -n ${{ parameters.clusterName }}'
          
        - bash: |
            # ./install-and-execute-testkube.sh AzureClientId=${{ parameters.clientId }}
            echo "Install testkube CLI"
            wget -qO - https://repo.testkube.io/key.pub | sudo apt-key add -
            echo "deb https://repo.testkube.io/linux linux main" | sudo tee -a /etc/apt/sources.list
            sudo apt-get update
            sudo apt-get install -y testkube

            echo "Install testkube on the cluster"
            helm repo add kubeshop https://kubeshop.github.io/helm-charts
            helm repo update
            helm upgrade --install --create-namespace testkube kubeshop/testkube -n testkube -f ./testkube/helm-testkube-values.yaml

            echo "Install testkube CRIs"
            export AZURE_CLIENT_ID=${{ parameters.clientId }}
            export AZURE_TENANT_ID=$(AzureTenantId)
            export WEBHOOK_URI=$(TeamsWebhookUri)
            
            envsubst < ./testkube-teams-integration.yaml > ./testkube-teams-integration-updated.yaml
            kubectl apply -f ./testkube-teams-integration-updated.yaml
            kubectl apply -f ./api-server-permissions.yaml
            envsubst < ./testkube-test-crs.yaml > ./testkube-test-crs-updated.yaml
            kubectl apply -f ./testkube-test-crs-updated.yaml

            echo "Wait for cluster to be ready"
            sleep 120

            echo "Run testkube tests"
            # Run the full test suite
            kubectl testkube run testsuite e2e-tests-merge --job-template ./custom-job-template.yaml --verbose

            # Get the current id of the test suite now running
            execution_id=$(kubectl testkube get testsuiteexecutions --test-suite e2e-tests-merge --limit 1 | grep e2e-tests | awk '{print $1}')

            # Watch until the all the tests in the test suite finish
            kubectl testkube watch testsuiteexecution $execution_id

            # Get the results as a formatted json file
            kubectl testkube get testsuiteexecution $execution_id --output json > testkube-results.json

            # For any test that has failed, print out the Ginkgo logs
            if [[ $(jq -r '.status' testkube-results.json) == "failed" ]]; then

                # Get each test name and id that failed
                jq -r '.executeStepResults[].execute[] | select(.execution.executionResult.status=="failed") | "\(.execution.testName) \(.execution.id)"' testkube-results.json | while read line; do
                testName=$(echo $line | cut -d ' ' -f 1)
                id=$(echo $line | cut -d ' ' -f 2)
                echo "Test $testName failed. Test ID: $id"

                # Get the Ginkgo logs of the test
                kubectl testkube get execution $id > out 2>error.log

                # Remove superfluous logs of everything before the last occurence of 'go downloading'.
                # The actual errors can be viewed from the ADO run, instead of needing to view the testkube dashboard.
                cat error.log | tac | awk '/go: downloading/ {exit} 1' | tac
                done

                # Explicitly fail the ADO task since at least one test failed
                exit 1
            fi
          workingDirectory: $(Build.SourcesDirectory)/test/testkube/
          displayName: "Install and execute testkube tests"

- template: Testkube
  parameters:
    environmentName: "Monitoring-Model-Cluster-WCUS"
    resourceGroup: "monitoring-model-cluster-wcus"
    clusterName: "Monitoring-Model-Cluster-WCUS"
    clientId: "$(MonitoringModelClusterWCUSClientId)"

- template: Testkube
  parameters:
    environmentName: "Monitoring-Model-Cluster-WEU"
    resourceGroup: "Monitoring-Model-Cluster-WEU"
    clusterName: "Monitoring-Model-Cluster-WEU"
    clientId: "$(MonitoringModelClusterWEUClientId)"

- template: Testkube
  parameters:
    environmentName: "ci-logs-prod-aks-geneva-integration"
    resourceGroup: "ci-logs-prod-aks"
    clusterName: "ci-logs-prod-aks-geneva-integration"
    clientId: "$(AksGenevaIntegrationClientId)"

- template: Testkube
  parameters:
    environmentName: "ci-logs-prod-aks-geneva-integration-multi-tenancy"
    resourceGroup: "ci-logs-prod-aks"
    clusterName: "ci-logs-prod-aks-geneva-integration-multi-tenancy"
    clientId: "$(AksGenevaIntegrationMultiTenancyClientId)"

- template: Testkube
  parameters:
    environmentName: "ci-logs-prod-aks-work-load-identity"
    resourceGroup: "ci-logs-prod-aks"
    clusterName: "ci-logs-prod-aks-work-load-identity"
    clientId: "$(AksWorkLoadIdentityClientId)"

- template: Testkube
  parameters:
    environmentName: "ci-logs-prod-wcus-fips"
    resourceGroup: "ci-logs-prod-aks"
    clusterName: "ci-logs-prod-wcus-fips"
    clientId: "$(WcusFipsClientId)"