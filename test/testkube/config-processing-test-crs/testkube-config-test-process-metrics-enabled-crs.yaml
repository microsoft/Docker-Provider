apiVersion: testworkflows.testkube.io/v1
kind: TestWorkflow
metadata:
  name: config-test-process-metrics-enabled
  namespace: testkube
spec:
  config:
    GOTOOLCHAIN:
      type: string
      default: ""
  content:
    git:
      uri: https://github.com/microsoft/Docker-Provider/
      revision: ci_prod
      paths:
      - test/ginkgo-e2e
  steps:
    - name: Run test
      container:
        workingDir: /data/repo/test/ginkgo-e2e
        image: cerebro31/ginkgo-test:latest
        env:
        - name: GOTOOLCHAIN
          value: "{{config.GOTOOLCHAIN}}"
      shell: |
        ginkgo run -r --label-filter "config-processing-process-metrics-enabled" ./configprocessing
  pod:
    nodeSelector:
      kubernetes.io/os: linux
      kubernetes.io/arch: amd64
