java-test-app has been deployed!

To check the service:
  kubectl get service java-test-app-service

To view the logs:
  kubectl logs -f deployment/java-test-app

The app is running on port {{ .Values.port }}.