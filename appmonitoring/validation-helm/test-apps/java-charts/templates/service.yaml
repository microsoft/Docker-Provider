apiVersion: v1
kind: Service
metadata:
  name: java-test-app-service
  labels:
    app: java-test-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      protocol: TCP
      targetPort: {{ .Values.port }}
  selector:
    app: java-test-app