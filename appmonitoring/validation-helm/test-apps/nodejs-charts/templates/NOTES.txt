{{ .Values.appName }} has been deployed!

To check the service:
  kubectl get service {{ .Values.appName }}-service

To view the logs:
  kubectl logs -f deployment/{{ .Values.appName }}

The app is running on port {{ .Values.port }} with endpoints:
- GET / - Returns a simple message
- GET /call-target - Calls the target URL: {{ .Values.targetUrl }}

Note: The /call-target endpoint has a 40% chance of simulating errors for testing purposes.