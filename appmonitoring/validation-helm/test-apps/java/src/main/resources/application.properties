# Set root logging level
logging.level.root=INFO
# Set your package logging to DEBUG for more details
logging.level.com.example=DEBUG
# Optional: show logs in color if supported
spring.output.ansi.enabled=ALWAYS