apiVersion: v1
kind: Service
metadata:
  name: python-test-app-service
  labels:
    app: python-test-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      protocol: TCP
      targetPort: {{ .Values.port }}
  selector:
    app: python-test-app
