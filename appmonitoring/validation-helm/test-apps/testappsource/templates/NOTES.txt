testappsource has been deployed!

To check the service:
  kubectl get service testappsource-service

To view the logs:
  kubectl logs -f deployment/testappsource

The app is running on port {{ .Values.port }} and responds with: "{{ .Values.message }}"