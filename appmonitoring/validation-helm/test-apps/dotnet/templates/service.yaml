apiVersion: v1
kind: Service
metadata:
  name: dotnet-test-app-service
  labels:
    app: dotnet-test-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      protocol: TCP
      targetPort: {{ .Values.port }}
  selector:
    app: dotnet-test-app