dotnet-test-app has been deployed!

To check the service:
  kubectl get service dotnet-test-app-service

To view the logs:
  kubectl logs -f deployment/dotnet-test-app

The app is running on port {{ .Values.port }} with OpenTelemetry instrumentation {{ if .Values.openTelemetryInject }}enabled{{ else }}disabled{{ end }}.