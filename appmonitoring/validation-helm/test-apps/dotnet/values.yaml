# Default values for dotnet test app
image: appmonitoringaddontestacr.azurecr.io/demoaks-dotnet-app:latest

# Port the service runs on
port: 3001

# Target URL for calling other services
targetUrl: "http://testappsource-service.default.svc.cluster.local:3001"

# Enable OpenTelemetry instrumentation
openTelemetryInject: true